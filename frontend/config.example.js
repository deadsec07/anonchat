// Copy to config.js and set your WebSocket URL.
// For local dev server: ws://localhost:3001
// For AWS API Gateway: wss://<api-id>.execute-api.<region>.amazonaws.com/$default
window.CONFIG = {
  wsUrl: 'ws://localhost:3001',
};

